# Flux documentation information architecture (pointer)

**Canonical authoring contract:** [`docs/_contract/information-architecture.md`](_contract/information-architecture.md) — full tree, per-section responsibilities, emotional progression, and disclosure model.

**User-facing pages (rendered on the dashboard at `/docs`):** [`docs/pages/`](pages/) — Markdown source, one file per URL slug (see [`docs/pages/index.md`](pages/index.md)).

**Voice, terminology, templates:** [`docs/_contract/`](_contract/) (`voice.md`, `terminology.md`, `page-template.md`, `diagrams.md`).

This file remains a short **entry point for repository readers** searching for “information architecture” at the repo root. The dashboard does not depend on this file for rendering.
