/** Flux MCP public contract version (semver). */
export const FLUX_MCP_CONTRACT_VERSION = "0.1.0";

/** Registered MCP tools in the v0.1 contract (excludes blocked destructive tools). */
export const FLUX_MCP_REGISTERED_TOOL_COUNT = 14;
