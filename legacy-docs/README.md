# Legacy documentation snapshots

This folder preserves **historical dashboard docs UI** and related components for operator reference. It is **not** the active documentation surface.

- **`dashboard/`** — Snapshot of the pre–information-architecture `/docs` implementation (single-page codex manual, shared styles, code block, and the pooled first-request guide page).

**Still live in the app (not archived):**

- `apps/dashboard/app/docs/v2-first-request/page.tsx` — product onboarding; may be replaced when `/docs/getting-started/first-request` exists.
- `apps/dashboard/src/lib/flux-codex-static.ts` — structured codex for CLI / AI; **not** legacy docs layout.

**Canonical IA:** [`docs/_contract/information-architecture.md`](../docs/_contract/information-architecture.md). **Rendered docs:** [`docs/pages/`](../docs/pages/).
