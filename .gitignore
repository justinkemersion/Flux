# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.js.map
*.d.ts
*.d.ts.map

# Local ad-hoc scripts / scratch work (not part of the Traefik or gateway stacks)
scratch/

# Traefik ACME store (path next to docker/traefik/docker-compose.yml)
docker/traefik/letsencrypt/

# Environment & secrets
.env
.env.*
!.env.example

# TypeScript cache
*.tsbuildinfo

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# k6 captured stdout from perf wrappers (keep *.summary.json / scorecard.*)
perf/results/**/*.stdout.txt

# Test coverage
coverage/
.nyc_output/

# Temporary files
tmp/
temp/

# Ad-hoc SQL kept locally (not the supported public→api path; use `flux push -s`)
packages/cli/migrations/move_to_api.sql

# Local database snapshots (initial baseline may be tracked intentionally)
database-backups/
